#include<stdio.h>
#define SIZE 10

int main()
{
 int arrInt[3]={2,4,6};
 char arrChar[3]={'A','B','C'};
 double arrDouble[3]={1224.35,3333.44,46346.46};
 
 printf("\nsizeof in array:%d",sizeof(arrInt));   
 printf("\nsizeof in array:%d",sizeof(arrChar));   
 printf("\nsizeof in array:%d",sizeof(arrDouble));   
 
 
 getch();
 return(0);
 
 //passing values by reference
 
 //Using a pointer to passing a function to a function
 
 //To retrieve multiple values from a function
 
 //To allocate Stings
 
 //To index array elements 
 
 //Retrieve a pointer to a file
  
 //Retrieve a pointer to a dynamically allocated piece of memory
 
 
}
